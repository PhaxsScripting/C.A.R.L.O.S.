# Hand models

Run `python3 holohand/scripts/fetch-models.py` from the repository root. The script
downloads the two pinned OpenCV Zoo models and verifies their SHA-256 hashes.
Weights are excluded from Git. Upstream licenses and documentation are retained
in `../vendor`.
