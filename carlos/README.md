# Carlos

Python assistant backend and Qt/QML desktop UI. See the [repository setup guide](../docs/SETUP.md).

The `ev` module, commands, IPC identifiers and data directories remain compatible with earlier E.V. installations.
